// import * as THREE from "three";
// const size = {
//   width: window.innerWidth,
//   height: window.innerHeight,
// };
// const aspectRatio = size.width / size.height;
// export const perspectiveCamera = new THREE.PerspectiveCamera(90, size.width / size.width, 0.1, 100);
// export const orthoCamera = new THREE.OrthographicCamera(-1 * aspectRatio, 1 * aspectRatio, 1, -1, 0.1, 100);
// perspectiveCamera.position.z = 3;
